# Deep Learning Course ud730



# Setup

Use a virtual environment

```
$ virtualenv env
$ source env/bin/activate
$ pip install -r requirements.txt
```

Also requires

```
$ sudo apt-get install python-tk
```

Then run everything

